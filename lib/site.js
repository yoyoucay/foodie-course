// Single source of truth for the canonical production origin, used by
// metadataBase, the sitemap, and structured data. Update this if the
// production domain changes.
export const SITE_URL = 'https://foodie.beemonswtf.web.id';
